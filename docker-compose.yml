version: "3.0"

services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - 8000:8000
    environment:
      GUNICORN_CMD_ARGS: "--bind=0.0.0.0:8000 --workers=3"
